---
import Katex from "katex";
import "katex/dist/katex.min.css";

export interface Props {
    formula: string;
    options?: Katex.KatexOptions;
}

const { formula, options } = Astro.props;
---

<Fragment set:html={Katex.renderToString(formula, options)} />
